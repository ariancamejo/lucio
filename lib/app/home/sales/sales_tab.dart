import 'package:flutter/material.dart';

class SalesTab extends StatelessWidget {
  const SalesTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Sales Tab"),
      ),
    );
  }
}
