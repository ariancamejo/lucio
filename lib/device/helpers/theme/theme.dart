import 'package:flutter/material.dart';

import './light.dart';
import './dark.dart';

ThemeData darkTheme(BuildContext context) => dark(context);

ThemeData lightTheme(BuildContext context) => light(context);
